# Developer Circles from facebook (Douala)

This is a simple js game with html5

# Run

- ```npm install``` to install dependency
- ```npx webpack``` to build
- ```./node_modules/.bin/webpack-dev-server``` to start server
